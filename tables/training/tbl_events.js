// Seed data.
const seeder = []

// create seed data.
for (let i = 1; i <= 0; i++) {

  seeder.push({
    // Todo: Add data to seed.
  })
}

module.exports = {
  "seeder": seeder,
  "indexes": [],
  "compoundIndexes": [],
  "table": "TBL_EVENTS"
}
